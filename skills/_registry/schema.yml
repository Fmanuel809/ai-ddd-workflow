version: 1
skill_schema:
  required:
    - id
    - version
    - title
    - owner
    - stage
    - inputs
    - outputs
    - quality_gates
    - files
  fields:
    id: string
    version: string
    title: string
    owner: string
    stage: string
    inputs: list
    outputs: list
    quality_gates: list
    files: list
